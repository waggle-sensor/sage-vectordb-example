apiVersion: v1
kind: Secret
metadata:
  name: huggingface-secret
  labels:
    app: weaviate
type: Opaque
data:
  # Base64 encoded Hugging Face token
  # To generate: echo -n "your_hf_token_here" | base64
  token: ""  # Replace with base64 encoded token
