apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sage

namePrefix: dev-commonobjectsbench-
commonLabels:
  benchmark: commonobjectsbench

# This overlay references the base and applies CommonObjectsBench-specific patches
resources:
  - ../../base

patches:
  # Patch job environment variables (CommonObjectsBench-specific + dev services)
  - path: env.yaml
    target:
      kind: Job
      labelSelector: "app=benchmark-job"

images:
  - name: PLACEHOLDER_BENCHMARK_JOB_IMAGE
    newName: gitlab-registry.nrp-nautilus.io/ndp/sage/nrp-image-search/benchmark-commonobjectsbench-job
    newTag: latest
