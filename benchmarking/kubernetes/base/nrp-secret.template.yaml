apiVersion: v1
kind: Secret
metadata:
  name: nrp-secret
  labels:
    app: benchmark
type: Opaque
data:
  # Base64 encoded NRP_API_KEY
  # To generate: echo -n "your_api_key_here" | base64
  NRP_API_KEY: ""  # Replace with base64 encoded API key