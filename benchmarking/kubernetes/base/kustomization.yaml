apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sage

resources:
  - benchmark-job.yaml
  - ._s3-secret.yaml
  - ._huggingface-secret.yaml

# Note: Vector DB and inference server configuration should be added
# in benchmark-specific overlays via environment variable patches

